{
  "name": "totonoe",
  "version": "1.1.1",
  "description": "ととのえ｜Totonoe — 日付のフォーマットをととのえる Chrome 拡張機能",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test \"src/**/*.test.js\"",
    "build": "rm -f totonoe.zip && zip -r -q totonoe.zip manifest.json icons src -x '*.test.js' && echo 'totonoe.zip を作成しました'",
    "bump": "node scripts/bump.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
